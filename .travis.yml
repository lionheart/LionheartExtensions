script:
- set -o pipefail && xcodebuild test -workspace Example/LionheartExtensions.xcworkspace -scheme LionheartExtensions -destination "platform=iOS Simulator,name=iPhone SE,OS=11.0" -destination-timeout 1 ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint

language: objective-c
osx_image: xcode9.0
branches:
  only:
  - master
